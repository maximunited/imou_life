# Test dependencies for Imou Life integration
# Note: pytest-homeassistant-custom-component removed due to Windows compatibility issues
# Camera component testing may be limited due to turbojpeg compatibility issues

# Python version compatibility: 3.11, 3.12, 3.13
# These dependencies are tested to work across all supported Python versions
# Minimum Python version: 3.11
# Maximum Python version: 3.13

# HTTP and async dependencies
aiohttp>=3.8.0; python_version>="3.11"

# Coverage
coverage>=7.4.0; python_version>="3.11"

# Home Assistant - Use a version range compatible with all Python versions
# Python 3.11: 2023.3+ (minimum supported)
# Python 3.12: 2024.2+ (minimum supported)  
# Python 3.13: 2025.1.0+ (minimum supported)
# Using 2024.2+ as it's compatible with Python 3.11-3.13
homeassistant>=2024.2.0; python_version>="3.11"
httpx>=0.24.0; python_version>="3.11"

# Integration runtime dependencies
imouapi==1.0.15; python_version>="3.11"

# Additional dependencies for component testing
numpy>=1.24.0; python_version>="3.11"

# Core testing
pytest>=8.0.0; python_version>="3.11"
pytest-asyncio>=0.24.0; python_version>="3.11"
pytest-cov>=6.0.0; python_version>="3.11"
pytest-mock>=3.12.0; python_version>="3.11"

# Optional: PyTurboJPEG for camera testing (may fail on some platforms)
# PyTurboJPEG>=1.6.0

# Note: pytest-homeassistant removed - using standard pytest instead
# This avoids Python version compatibility issues
